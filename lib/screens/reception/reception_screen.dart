import 'package:flutter/material.dart';

class ReceptionScreen extends StatelessWidget {
  const ReceptionScreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Reception"));
  }
}
